# Vault Helm Chart Value Overrides
global:
  enabled: true

server:
  ha:
    enabled: true
    replicas: 3

# Vault UI
ui:
  enabled: true
  serviceType: "LoadBalancer"
  serviceNodePort: null
  externalPort: 8200